# Default values for hr-app.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

nameOverride: ""
fullnameOverride: ""

frontend:
  replicaCount: 1
  
  image:
    repository: gcr.io/YOUR_PROJECT_ID/frontend-app
    pullPolicy: IfNotPresent
    tag: "latest"
  
  service:
    type: LoadBalancer
    port: 80
  
  resources: {}
    # limits:
    #   cpu: 500m
    #   memory: 512Mi
    # requests:
    #   cpu: 250m
    #   memory: 256Mi

backend:
  replicaCount: 1
  
  image:
    repository: gcr.io/YOUR_PROJECT_ID/backend-app
    pullPolicy: IfNotPresent
    tag: "latest"
  
  service:
    type: ClusterIP
    port: 8080
  
  resources: {}
    # limits:
    #   cpu: 500m
    #   memory: 512Mi
    # requests:
    #   cpu: 250m
    #   memory: 256Mi